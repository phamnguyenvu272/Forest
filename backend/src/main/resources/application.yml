spring:
  datasource:
    url: jdbc:mysql://localhost:3307/forest_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8080

app:
  jwt:
    secret: change-this-secret-change-this-secret-change-this
    expiration-ms: 86400000
